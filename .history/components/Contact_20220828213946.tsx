import React from 'react'

type AppProps=  {
    mediumName: string;
    link: string;
    image: string 
}

const Contact = ({ mediumName, link, image } : AppProps ) : JSX.Element => (
    <div>
        
    </div>
)

export default Contact;